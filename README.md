# 📄 Resume Ranker using NLP

## Description
Ranks resumes based on relevance to a given job description using NLP techniques (TF-IDF + cosine similarity).

## Setup
```bash
pip install -r requirements.txt
